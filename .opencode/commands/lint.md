---
description: Validate shell scripts with ShellCheck
agent: general
---

Run ShellCheck on all shell scripts in the project:

- Check `install/*.sh`
- Check `themes/*/backgrounds.sh`
- Check `boot.sh`, `install.sh`
- Check `migrations/*.sh`

Report any warnings or errors found.