#!/bin/bash

# ctxos:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/ctxos/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! ctxos-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
