#!/bin/bash

# ctxos:summary=Launch a floating terminal with the CtxOS presentation wrapper
# ctxos:args=<command>

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.ctxos.terminal --title=CtxOS -e bash -c "ctxos-show-logo; $cmd; if (( \$? != 130 )); then ctxos-show-done; fi"
