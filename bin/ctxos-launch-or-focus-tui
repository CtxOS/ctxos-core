#!/bin/bash

# ctxos:summary=Launch a TUI or focus an existing terminal window for it
# ctxos:args=<command> [args...]

APP_ID="org.ctxos.$(basename "$1")"
LAUNCH_COMMAND="ctxos-launch-tui $@"

exec ctxos-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
