#!/bin/bash

# ctxos:summary=Launch a TUI command in the default terminal with CtxOS styling
# ctxos:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.ctxos.$(basename $1) -e "$1" "${@:2}"
