#!/bin/bash

APP_NAME="$1"
if [[ -z "$APP_NAME" ]]; then
  echo "Usage: ctxos-launch-webapp <app-name>"
  exit 1
fi
exec chromium --new-window --ozone-platform=wayland --app="https://$APP_NAME" --name="$APP_NAME" --class="$APP_NAME"

