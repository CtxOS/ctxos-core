#!/bin/bash

# ctxos:summary=Returns true if all of the named packages are installed on the system (or false if any of them are missing).
# ctxos:args=<packages...>

for pkg in "$@"; do
  pacman -Q "$pkg" &>/dev/null || exit 1
done

exit 0
