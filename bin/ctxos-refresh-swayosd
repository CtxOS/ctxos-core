#!/bin/bash

# ctxos:summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the CtxOS defaults and restart the service.

ctxos-refresh-config swayosd/config.toml
ctxos-refresh-config swayosd/style.css
ctxos-restart-swayosd
