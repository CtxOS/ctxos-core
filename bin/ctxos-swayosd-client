#!/bin/bash

# ctxos:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# ctxos:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(ctxos-hyprland-monitor-focused)" "$@"
