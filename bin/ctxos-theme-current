#!/bin/bash

# ctxos:summary=Show current theme
# ctxos:examples=ctxos theme current

THEME_NAME_PATH="$HOME/.config/ctxos/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  cat $THEME_NAME_PATH | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g'
else
  echo "Unknown"
fi
