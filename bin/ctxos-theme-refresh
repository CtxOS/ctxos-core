#!/bin/bash

# ctxos:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/ctxos/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  CTXOS_THEME_SKIP_BACKGROUND=1 ctxos-theme-set "$(cat $THEME_NAME_PATH)"
fi
