#!/bin/bash

# ctxos:summary=Toggle Waybar visibility
# ctxos:examples=ctxos toggle waybar

ctxos-toggle waybar-off

if pgrep -x waybar >/dev/null; then
  pkill -9 -x waybar
else
  uwsm-app -- waybar >/dev/null 2>&1 &
fi
