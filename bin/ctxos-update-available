#!/bin/bash

CURRENT=$(git -C ~/.local/share/ctxos log -1 --format=%cd --date=unix 2>/dev/null || echo 0)
LATEST=$(git ls-remote --heads origin master 2>/dev/null | awk "{print \$1}")
if [[ "$CURRENT" != "$LATEST" ]]; then
  echo "Update available"
else
  echo "Up to date"
fi

