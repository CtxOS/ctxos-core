#!/bin/bash

# ctxos:summary=Print the active CtxOS mirror and package channel

if grep -q "https://stable-mirror.ctxos.github.io/" /etc/pacman.d/mirrorlist; then
  mirror="stable"
elif grep -q "https://rc-mirror.ctxos.github.io/" /etc/pacman.d/mirrorlist; then
  mirror="rc"
elif grep -q "https://mirror.ctxos.github.io/" /etc/pacman.d/mirrorlist; then
  mirror="edge"
else
  mirror="unknown"
fi

if grep -q "https://pkgs.ctxos.github.io/stable/" /etc/pacman.conf; then
  pkgs="stable"
elif grep -q "https://pkgs.ctxos.github.io/edge/" /etc/pacman.conf; then
  pkgs="edge"
elif grep -q "https://pkgs.ctxos.github.io/rc/" /etc/pacman.conf; then
  pkgs="rc"
else
  pkgs="unknown"
fi

if [[ $mirror == $pkgs ]]; then
  echo $mirror
else
  echo "$mirror / $pkgs"
fi
