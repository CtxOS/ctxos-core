# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(ctxos-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, ctxos-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(ctxos-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, ctxos-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, ctxos-launch-browser --private
bindd = SUPER SHIFT, N, Editor, exec, ctxos-launch-editor

# Add extra bindings
# bindd = SUPER SHIFT, A, ChatGPT, exec, ctxos-launch-webapp "https://chatgpt.com"
# bindd = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Overwrite existing bindings, like putting CtxOS Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, CtxOS menu, exec, ctxos-menu
